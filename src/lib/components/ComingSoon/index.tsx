/* eslint-disable import/extensions */
/* eslint no-console: ["error", { allow: ["warn", "error"] }] */

import ComingSoonComponent from '@/lib/components/ComingSoon/ComingSoon';

export default function ComingSoon() {
  return (
    <div className="h-screen">
      <ComingSoonComponent />
    </div>
  );
}
