/* eslint-disable import/extensions */
/* eslint no-console: ["error", { allow: ["warn", "error"] }] */

'use client';

import WelcomeLigaComponent from '@/lib/components/WelcomeLiga/WelcomeLiga';

export default function WelcomeLiga() {
  return (
    <div>
      <WelcomeLigaComponent />
    </div>
  );
}
